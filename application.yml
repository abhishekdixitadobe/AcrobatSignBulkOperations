server:
  port: 8090

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB        
  application:
    name: Doc Sign Application
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
integration-key: 
createContractPath: src/main/resources/contract/exitForm/index.zip
downloadPath: output/
baseUrl: https://abhishekdixitg.in1.adobesign.com
agreement_status: SIGNED,CANCELLED,EXPIRED,ACCEPTED,APPROVED,DISABLED

agreement_type: AGREEMENT, MEGASIGN_CHILD, WIDGET_INSTANCE, MEGASIGN_PARENT